# Max for Live Theme

- Live uses the theme set under `Preferences > Color and Theme > Color Theme` if launched directly.
- If launched from Live (e.g., via `Edit in Max ), then the Live theme takes over, note that the Live theme takes over both for the new window *and all existing windows*.