# Max for Live

- **Device Height**: `342px`
- To prevent borders around the device, set the `Initial Window Size` to the width and height of the device (using an origin of `0., 0.`
- Set `Device Inspector > Color > Locked Patcher Background Color > Dynamic > Live Theme Colors: Control Header` to match the background default Live devices use
