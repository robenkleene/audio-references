# Max Window Management

- `⇧⌘I`: Open `Inspector Window`
- `⌘I`: Open `Inspector` in the `Sidebar`
- `⇧⌘M`: Open the `Max Console`
- `⌘M`: Open the `Sidebar Max Console`
- Focus can be either in the `Patcher Window` or the `Sidebar`, there's no way to toggle focus without using the mouse, but closing the 
